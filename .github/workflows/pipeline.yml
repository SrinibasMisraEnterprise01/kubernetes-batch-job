name: Proof HTML
on:
  push:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu
    strategy:
      fail-fast: false
      matrix:
        cloud:
        - gcp
        environment:
        - dev
        # - stage
        # - prod
        location:
        - us-west2
        - us-east4
    steps:
      - uses: anishathalye/proof-html@v1.1.0
        with:
          directory: ./
